The figure on this page is an interactive activity that checks the learner’s understanding of the following networked host services. Web Services, the hosts use HTTP or HTTPS to access this service. Authentication Services, Network devices are controlled through AAA. DHCP, this service assigns IP addressing information to hosts automatically. Email Services, the client sends and retrieves the message using these protocols: SMTP, POP, or IMAP. Print Services, this service enables multiple users to access a printer. DNS, this service translates domain names into IP addresses. Intrusion Detection and Prevention Services, this service monitors and processes network traffic. File Services, this service uses the FTP protocol to transfer data between a server and a client.